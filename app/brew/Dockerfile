ARG DOCKERHUB_ARCH
ARG ROOTFS_VERSION
ARG MATRIX_ROOTFS

FROM docker.io/${DOCKERHUB_ARCH}/ruby:${ROOTFS_VERSION}

ARG VERSION
ARG MATRIX_ROOTFS
ARG MATRIX_ARCH

ENV HOMEBREW_NO_ANALYTICS=1
ENV HOMEBREW_NO_AUTO_UPDATE=1

COPY app/brew/setup.sh /setup.sh
RUN sh /setup.sh && rm /setup.sh
