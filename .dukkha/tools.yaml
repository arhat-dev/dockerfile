bootstrap: {}

tools:
  docker:
  - name: buildkit
    env:
    - DOCKER_BUILDKIT=1
    cmd: [docker]

  - name: buildah
    cmd: [buildah]
    buildCmd: [bud]

shells:
- name: sh
  env:
  - DEBIAN_VERSION=buster
  - ALPINE_VERSION=3.13
  - GO_VERSION=1.16
  cmd: [sh]
